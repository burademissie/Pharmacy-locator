function About() {
  window.scrollTo({
    top: document.body.scrollHeight * 0.3,
    behavior: 'smooth'
  });
}

function Contact() {
  window.scrollTo({
    top: document.body.scrollHeight * 0.9999,
    behavior: 'smooth' 
  });
}